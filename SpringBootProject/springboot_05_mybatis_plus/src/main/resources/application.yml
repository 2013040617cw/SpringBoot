#相关信息的配置
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssm_db
    username: root
    password: 123456

    #设置MP相关的配置
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tb1_
